{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "SUPABASE_URL",
      "description": "Supabase project URL (https://<project>.supabase.co). Required at boot \u2014 the client/server Supabase clients throw without it.",
      "requiredAtBoot": true
    },
    {
      "name": "SUPABASE_PUBLISHABLE_KEY",
      "description": "Supabase publishable (anon) key. Required at boot \u2014 used by the browser client and the SSR auth middleware.",
      "requiredAtBoot": true
    },
    {
      "name": "SUPABASE_SERVICE_ROLE_KEY",
      "description": "Supabase service_role key for server-side admin ops (src/integrations/supabase/client.server.ts): full character load, retry replies. Optional at boot; required for admin/character management.",
      "requiredAtBoot": false
    },
    {
      "name": "GROQ_API_KEY",
      "description": "Groq API key for server-side chat replies (src/lib/ai/provider.ts). Optional at boot; required for the chat AI feature.",
      "requiredAtBoot": false
    }
  ]
}
