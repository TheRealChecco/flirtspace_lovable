# Base44 dev placeholders — overridden by /run/base44/app.env (platform secrets).
# These are only used on-demand (chat replies / admin ops); the app boots fine empty.
GROQ_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
